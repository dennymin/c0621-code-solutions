/* exported getValue */
function getValue(object, key) {
  return object[key];
}

// I am given an object and a key
// Because I know which key I want to find as it is given to me, I simply need
// to find that value at that specific key within the object
