/* exported getLastChar */
function getLastChar(string) {
  return string[string.length - 1];
}

// my thought process was that I have to identify the last element and then return it; so my code reflects that
