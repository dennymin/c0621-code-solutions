/* exported isLowerCased */
function isLowerCased(word) {
  if (word !== word.toLowerCase()) {
    return false;
  } else {
    return true;
  }
}

// this will be a similar process to the upper case function but with lower case now
